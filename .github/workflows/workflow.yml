name: Main workflow

on:
  pull_request:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ${{ matrix.operating-system }}
    strategy:
      matrix:
        operating-system: [windows-latest]
    steps:
      - uses: actions/checkout@v3
      - uses: actions/setup-node@v3
        with:
          node-version: '14'
          registry-url: https://registry.npmjs.org/
      - name: Install
      - run: npm config set @bentley:registry=https://pkgs.dev.azure.com/bentleycs/_packaging/Packages/npm/registry/
      - run: npm config set always-auth=true
      - run: npm install -g vsts-npm-auth --registry https://registry.npmjs.com --always-auth false
      - name: Install
        working-directory: ./typescript
        run: vsts-npm-auth -config .npmrc -f -n
      - name: Install
        working-directory: ./typescript
        run: npm install
      - name: Typescript compiler
        working-directory: ./typescript
        run: npx tsc
      - name: EsLint
        working-directory: ./typescript
        run: npm run lint