name: Main workflow

on:
  pull_request:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ${{ matrix.operating-system }}
    strategy:
      matrix:
        operating-system: [windows-latest]
    steps:
      - uses: actions/checkout@v3
      - uses: actions/setup-node@v3
        with:
          node-version: '14'
          registry-url: https://pkgs.dev.azure.com/bentleycs/_packaging/Packages/npm/registry/
      - name: test
        run: | 
          //pkgs.dev.azure.com/bentleycs/_packaging/Packages/npm/registry/:username=VssSessionToken
          //pkgs.dev.azure.com/bentleycs/_packaging/Packages/npm/registry/:_password="anhveTdhZzJpN2V1YWlmdDJlaG1teWJ4NDJiNGw0aDR4aWl6NXJkeHR3cTNiczVoZm5kcQ=="
          //pkgs.dev.azure.com/bentleycs/_packaging/Packages/npm/registry/:email=not-used@example.com
      - name: Install
        working-directory: ./typescript
        run: npm install
      - name: Typescript compiler
        working-directory: ./typescript
        run: npx tsc
      - name: EsLint
        working-directory: ./typescript
        run: npm run lint