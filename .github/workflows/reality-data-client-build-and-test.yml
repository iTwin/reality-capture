name: (RDCBuildTest) Build and test reality-data-client

on:
  workflow_call:
    inputs:
      node-version:
        required: true
        type: string
      os:
        required: true
        type: string

jobs:
  build_and_test_sdk:
    runs-on: ${{ inputs.os }}

    steps:
      - name: Checkout github repo (+ download lfs dependencies)
        uses: actions/checkout@v5
        with:
          lfs: true

      - name: Checkout LFS objects
        run: git lfs checkout

      - name: Install pnpm
        uses: pnpm/action-setup@v6
        with:
          version: 9

      - name: Setup Node.js ${{ inputs.node-version }}
        uses: actions/setup-node@v6
        with:
          node-version: ${{ inputs.node-version }}

      - name: Install reality-capture dependencies
        working-directory: ./typescript/packages/reality-capture
        run: pnpm install --frozen-lockfile=true

      - name: Build reality-capture
        working-directory: ./typescript/packages/reality-capture
        run: pnpm build

      - name: Install dependencies
        working-directory: ./typescript/packages/reality-data-client
        run: pnpm install --frozen-lockfile=true

      - name: Build
        working-directory: ./typescript/packages/reality-data-client
        run: pnpm build

      - name: EsLint
        working-directory: ./typescript/packages/reality-data-client
        run: pnpm lint

      - name: Run unit tests
        working-directory: ./typescript/packages/reality-data-client
        run: pnpm test:unit
