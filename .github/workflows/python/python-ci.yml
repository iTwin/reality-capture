name: Python SDK CI

on:
  push:
    paths:
      - "python_sdk/**"
    branches: [ "feature/2.0.0", "feature/2.0.0-release", "feature/2.0.0-ts", "main" ]
  pull_request:
    paths: 
      - "python_sdk/**"
    branches: [ "feature/2.0.0", "feature/2.0.0-release", "main" ]
  workflow_dispatch:

jobs:
  build_and_test_sdk:
    strategy:
      matrix:
        python-version: ['3.10', '3.11', '3.12', '3.13']
        os: [ubuntu-latest, windows-latest, macos-latest]
      fail-fast: false
      
    uses: ./.github/workflows/steps/python-build-and-test.yml
    with:
      python-version: ${{ matrix.python-version }}
      os: ${{ matrix.os }}
